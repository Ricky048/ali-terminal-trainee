// 用于暂存天气数据的vuex空间
export default {
  namespaced: true,
  state: () => ({
    // weather: JSON.parse()
    // weather: this.getRealTimeWeather()
  }),

  mutations: {
    saveRealTimeDataToStorage() {
      // this.
    }
  }
}
